apply plugin: 'application'
apply plugin: 'java'

mainClassName = "de.nimple.greendao.Generator"
outputDir = "../nimpleandroid/src/main/java"

dependencies {
    compile('de.greenrobot:greendao-generator:1.3.1')
}

run {
    args outputDir
}
